# Aponta o SSH_AUTH_SOCK para o agente SSH do gcr (gcr-ssh-agent.socket).
# Lido pelo systemd user manager (o niri sobe via niri-session, que importa
# o environment), então todo app da sessão herda esta variável.
SSH_AUTH_SOCK=${XDG_RUNTIME_DIR}/gcr/ssh
