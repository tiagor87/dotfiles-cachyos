# Backups criados pelos symlinks
*.bak
*.bak.*

# Logs temporários de instalação
dotfiles-install-log.*

# Lixo de editor / SO
*.swp
*~
.DS_Store

# MCPs / ferramentas locais (artefatos de projeto, não versionar)
.serena/
.tokensave/
.claude/

# Segredos / overrides locais (nunca versionar — ver ~/.zshenv)
*.local
.env
.env.*
*.secret
secrets.*
