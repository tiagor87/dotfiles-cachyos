# Backups criados pelos symlinks
*.bak
*.bak.*

# Logs temporários de instalação
dotfiles-install-log.*

# Lixo de editor / SO
*.swp
*~
.DS_Store

# MCPs / ferramentas locais (artefatos de projeto, não versionar)
.serena/
.tokensave/
.claude/
# .headroom/: o `--memory` cria um SQLite por workspace aqui (memory.db +
# memory_graph.db + memory_vectors.db, na casa dos MB).
.headroom/

# Segredos / overrides locais (nunca versionar — ver ~/.zshenv)
*.local
.env
.env.*
*.secret
secrets.*
