# Tema Catppuccin Mocha para o Limine — bloco de opções GLOBAIS.
#
# IMPORTANTE: no formato novo do Limine (limine.conf), opções globais devem
# ficar no TOPO do arquivo, ANTES da primeira entrada de kernel. Este bloco é
# injetado lá pelo boot/install/1-limine-theme.sh (com backup do limine.conf).
#
# Paleta = mesmas cores do fallback do kitty (theme.conf), pra um boot → login
# → desktop coeso (Catppuccin Mocha / roxo).

# >>> dotfiles theme (Catppuccin Mocha)
term_palette: 45475A;F38BA8;A6E3A1;F9E2AF;89B4FA;F5C2E7;94E2D5;BAC2DE
term_palette_bright: 585B70;F38BA8;A6E3A1;F9E2AF;89B4FA;F5C2E7;94E2D5;A6ADC8
term_background: 1E1E2E
term_foreground: CDD6F4
backdrop: 1E1E2E
# <<< dotfiles theme
